import { FossilIdsPipe } from './fossil-ids.pipe';

describe('FossilIdsPipe', () => {
  it('create an instance', () => {
    const pipe = new FossilIdsPipe();
    expect(pipe).toBeTruthy();
  });
});
